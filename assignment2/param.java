// Class representing a function parameter
public class param {
    // Fields to store information about the parameter
    public String id;    // Parameter identifier name
    public String type;  // Type of the parameter

    // Constructor to initialize the parameter with provided values
    public param(String id, String type) {
        this.id = id;
        this.type = type;
    }
}
